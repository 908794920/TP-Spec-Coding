---
artifact: implementation
task_id: ""
artifact_contract:
  version: 5.3.0
---

# Implementation（按需）

简单单文件任务可只写：`入口 / 复用 / 修改 / 不做 / 验证` 五行。跨文件、遗留重构、外部 AI 交接或存在多条实现路线时使用下列完整结构。

## 开发前：路线与边界

### 目标

### 现状与入口

### Usage Footprint
- 静态调用：
- Spring/反射/XML/配置/JSP/模板/消息/定时任务等动态引用：
- 外部接口、数据和兼容契约：

### 可复用代码
- 可直接复用：
- 可修改后复用：
- 已存在的重复实现：

### 事实前提修正
- 被真实代码推翻的 Requirement/Architecture 前提：无 / 说明：

### 选择的修改方式
- 直接复用 / 修改现有实现 / 替换或删除重复实现 / 新增最小实现：
- 选择理由：

### 修改边界
允许修改：

### 明确不做
- 不新增无当前调用方的扩展点；
- 不重构无关模块；
- 不改变未授权接口/数据/权限语义；

### 可读性与中文注释
- 非显而易见 Why/Constraint/Risk 的中文注释位置：

### 验证与清理
- 非可视化验证：
- 可视化验证：
- durable regression test：
- temporary diagnostic test / Temp：

### 变更理由映射
- 预计修改文件 → AC / Finding / Migration：
- 新依赖：无 / 明确理由：
- 新配置项：无 / 明确理由：
- 新抽象层：无 / 明确理由：
- 新兼容路径：无 / 当前真实调用方：

## 开发后：实际结果

### 实际改动

### 与路线的偏差及原因

### 简化与删除
- 本次变更替代的旧路径：
- 删除的本次 orphan / duplicate / debug residue：
- 仍为“以后可能使用”保留的代码：否 / 明确现实依据：

### 自测与证据

### 已知风险 / 未完成项
